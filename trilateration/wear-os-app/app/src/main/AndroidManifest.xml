<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Wear OS feature -->
    <uses-feature android:name="android.hardware.type.watch" />

    <!-- Sensor permissions -->
    <uses-permission android:name="android.permission.BODY_SENSORS" />
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />
    <uses-permission android:name="android.permission.HIGH_SAMPLING_RATE_SENSORS" />

    <!-- Location permissions (GPS clustering) -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Notifications (Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Foreground service (continuous monitoring) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_HEALTH" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.DeviceDefault"
        tools:targetApi="34">
        <!-- BUG+041 fix: allowBackup was true, meaning the cigarette/drink
             detection SQLite DB would be synced to Google Drive without
             the user's explicit consent. For a privacy-sensitive health
             tracker that markets "local only", this was a direct leak.
             Set to false; if a Google-Drive backup feature is added later,
             it must go through an explicit in-app consent flow. -->

        <!-- Main Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.DeviceDefault">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Detection Service (foreground).
             BUG+040 fix: exported was true in the main manifest which
             let ANY app on the watch fire ACTION_START / ACTION_STOP /
             ACTION_BOOST Intents at our service. Now exported=false so
             only our own package can call it. Dev workflows that need
             `adb shell am start-foreground-service` should override this
             via src/debug/AndroidManifest.xml with an intent-filter +
             exported=true (debug build variant only). -->
        <service
            android:name=".DetectionService"
            android:enabled="true"
            android:exported="false"
            android:process=":detection"
            android:foregroundServiceType="health" />

        <!-- Wear OS capabilities -->
        <meta-data
            android:name="com.google.android.wearable.standalone"
            android:value="true" />

    </application>

</manifest>
